setup:
  addons:
    - plan: cleardb:ignite
  config:
    ASPNETCORE_ENVIRONMENT: Production

build:
  docker:
    web: Dockerfile-backend
    # Si vous voulez déployer le frontend, commentez la ligne au-dessus et décommentez celle-ci
    # web: Dockerfile-frontend

run:
  web: dotnet VotreApp.dll